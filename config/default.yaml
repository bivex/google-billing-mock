server:
  port: 8080
  read_timeout: 30s
  write_timeout: 30s
  shutdown_timeout: 10s

mock:
  scenarios_path: config/scenarios/default.json
  default_latency_ms: 0
  error_rate: 0.0

log:
  level: info

metrics:
  enabled: true
  path: /metrics

auth:
  mock_enabled: false
